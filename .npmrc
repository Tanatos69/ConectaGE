# Don't run a full `pnpm install` before every `pnpm run` script.
# Avoids the optional `unrs-resolver` native-build warning (it falls back to a
# JS resolver) blocking `pnpm --filter web dev`.
verify-deps-before-run=false
